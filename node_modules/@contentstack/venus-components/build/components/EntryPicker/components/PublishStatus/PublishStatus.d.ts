import React from "react";
import { PublishStatus as IPublishStatus } from "../../types";
import "./PublishStatus.css";
export interface PublishStatusProps {
    publishStatuses: IPublishStatus[];
    currentVersion?: string;
    currentLocale: string;
}
export declare const PublishStatus: (props: PublishStatusProps) => React.JSX.Element;
