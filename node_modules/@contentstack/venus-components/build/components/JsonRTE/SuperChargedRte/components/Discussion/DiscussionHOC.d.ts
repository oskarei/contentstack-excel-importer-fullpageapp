import React from 'react';
import { BaseEditor, Path } from 'slate';
import { IDiscussionProps, IElement } from '../../utils/types';
export declare const DiscussionHOC: (RegistryElement: any, discussion: IDiscussionProps, editor: BaseEditor, element: IElement, path: Path) => (props: any) => React.JSX.Element;
