import React from 'react';
import { ChooseAssetModalProps } from '../../types';
interface SelectImageProps extends Omit<ChooseAssetModalProps, "onSubmit" | "closeModal" | "onCancel"> {
    isEmbed?: boolean;
    minSize?: number;
    maxSize?: number;
    setImages?: (assets: any) => void;
}
declare function SelectImage(props: SelectImageProps): React.JSX.Element;
export default SelectImage;
