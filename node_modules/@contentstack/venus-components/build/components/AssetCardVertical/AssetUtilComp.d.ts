import React from "react";
type AssetWrapperProps = {
    version?: 'v1' | 'v2';
    children: React.ReactNode;
    className?: string;
};
export declare const AssetWrapper: ({ version, children, className }: AssetWrapperProps) => React.JSX.Element;
export {};
