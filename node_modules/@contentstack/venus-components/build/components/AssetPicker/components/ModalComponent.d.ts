import React from 'react';
import '../style.css';
import { ChooseAssetModalProps } from '../types';
export declare const getImageMarkdown: (href: any, text?: string) => string;
declare global {
    interface Window {
        extension_uid: any;
    }
}
declare const ModalComponent: (props: ChooseAssetModalProps) => React.JSX.Element;
export default ModalComponent;
