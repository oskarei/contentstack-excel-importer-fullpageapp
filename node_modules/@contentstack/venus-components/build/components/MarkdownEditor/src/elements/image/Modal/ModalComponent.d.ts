import React from 'react';
import './style.css';
import { Editor } from '../../../lib/node-pagedown';
export declare type ModalComponentProps = {
    editor: typeof Editor;
    closeModal: Function;
    state: Record<string, any>;
};
export declare const insertImage: ({ editor, url, state }: {
    editor: any;
    url: any;
    state: any;
}) => void;
export default class extends React.Component<ModalComponentProps> {
    state: {
        image: {
            url: string;
            filename: string;
        };
    };
    setImage: (asset: any) => void;
    handleSubmit: () => void;
    handleReset: () => void;
    render(): React.JSX.Element;
}
