import React from "react";
import { PublishStatus } from "../../types";
import "./PublishDetailsModal.css";
interface PublishDetailsModalProps {
    publishStatuses: PublishStatus[];
    closeModal: () => undefined;
}
export declare function PublishDetailsModal(props: PublishDetailsModalProps): React.JSX.Element;
export {};
