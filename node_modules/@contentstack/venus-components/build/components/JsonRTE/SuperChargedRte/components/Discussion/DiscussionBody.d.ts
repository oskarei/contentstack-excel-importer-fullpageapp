import React from 'react';
import { IEditorRequestProps, IMessageDTO, IUserState } from '../../utils/types';
interface IDiscussionBody {
    isLoading: boolean;
    handleOnSave: React.MutableRefObject<any>;
    userState: IUserState;
    comments: Array<IMessageDTO>;
    fetchingMore: boolean;
    editComment: string;
    editorMetadata: IEditorRequestProps;
}
declare const DiscussionBody: React.FC<IDiscussionBody>;
export default DiscussionBody;
