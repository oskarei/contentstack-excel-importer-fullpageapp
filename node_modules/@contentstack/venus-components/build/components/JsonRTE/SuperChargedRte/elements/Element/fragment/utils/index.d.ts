import { Editor, Path, Node } from 'slate';
/**
 *
 * @returns If the given node can be selected to add `{ break: true }`
 */
export declare function checkIfBreakIsNeeded(editor: Editor, node: Node, path: Path): boolean;
/**
 *
 * @returns If the given node can be selected to remove `{ break: true }`
 */
export declare function checkIfBreakIsNeededToRemove(editor: Editor, node: Node, path: Path): boolean;
