import React from 'react';
interface ColorItem {
    color: string;
    pixel_percentage: number;
}
interface FieldConfig {
    uid: string;
    title?: string;
    type: string;
    threshold?: boolean;
    threshold_value?: number;
    hidden?: boolean;
    showConfidence?: boolean;
}
type ListColorProps = {
    field: FieldConfig;
    bgColor: ColorItem[];
    dominantColors: ColorItem[];
    threshold_value?: number;
    showAll?: boolean;
    showConfidence?: boolean;
};
declare const ListColor: ({ field, bgColor, dominantColors, threshold_value, showAll }: ListColorProps) => React.JSX.Element;
export default ListColor;
