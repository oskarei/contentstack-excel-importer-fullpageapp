import React from 'react';
interface AssetBreadCrumbItem {
    uid: string;
    displayName: string;
    action: (breadCrumbIndex: number, uid: string) => void;
}
export declare function AssetBreadCrumb({ breadcrumbs, showIcon }: {
    breadcrumbs: AssetBreadCrumbItem[];
    showIcon?: boolean;
}): React.JSX.Element;
export {};
